package fileOutputStream;

import org.junit.Ignore;
import org.junit.Test;

public class MultiTableToFileTest {

    /**
     * Тестируем сразу оба метода
     * 1) расчет таблицы умножения
     * 2) вывод в файл
     * Тест прошёл успешно,
     * Выключил, чтобы не было конфликтов с другими заданиями
     */
    @Ignore
    @Test
    public void test() {
        MultiTableToFile.writeToFile(5);
    }
}